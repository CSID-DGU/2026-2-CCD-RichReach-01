# Frontend

Flutter 클라이언트입니다.

- 내부 구조와 플랫폼(웹/앱)은 프론트 팀이 결정합니다.
- Flutter 프로젝트는 이 디렉터리 안에서 `flutter create .`으로 생성합니다. (`pubspec.yaml`이 `frontend/`에 위치해야 CI가 동작합니다.)
- CI: [frontend-ci.yml](../.github/workflows/frontend-ci.yml) (`flutter analyze`, `flutter test`)

## 실행 방법

TODO: 프로젝트 생성 후 작성
